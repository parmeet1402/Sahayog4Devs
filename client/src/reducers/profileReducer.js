const initialState = {
  profile: null,
  userSkills: null,
  requiredSkills: null,
  loading: false
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
